export default function SectionDivider() {
  return (
    <div
      className="h-px max-w-[600px] mx-auto"
      style={{
        background: "linear-gradient(to right, transparent, rgba(42, 138, 85, 0.3), transparent)",
      }}
    />
  );
}
